require('dotenv').config()
const { query } = require('./client')

// const data = query(
// `CREATE TABLE users (
// name text
// );`
// ).then(() => {
// return query(`INSERT INTO users (name) values ('craig')`)
// })
